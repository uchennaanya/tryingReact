import React from 'react'
import {LIST} from './listDetail.js'

class extends List React.Component {

  constructor(props) {
    super(props)
    this.state = {
      list: lists
    }

  }

  this.state.map((item, idex) => <p> {this.state.name} </p>)

  render() {

    return (
      <div>
        
      </div>
    )
  }
}
